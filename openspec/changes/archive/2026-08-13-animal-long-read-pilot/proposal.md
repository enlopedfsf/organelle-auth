# Archive record: animal-long-read-pilot

## Final status

`FAILED` → `SUPERSEDED BY animal-lr-recruitment-diagnostics`

This first animal ONT pilot did not produce a valid consensus candidate. The failure is
retained as an auditable experiment and is superseded by the diagnostic closeout, which
resolved the incomplete Flye runtime and recruitment semantics before the controlled reruns.

## Scientific disposition

The pilot never entered `IDENTIFY` or `DECISION`. Animal ONT remains experimental and the
formal topology remains `INCONCLUSIVE`. PMAT2 remains gated by Issue #10.

## Provenance

The original proposal, design, tasks, animal spec delta, and lightweight preflight artifact
are preserved. Large FASTQ/BAM/work products remain local and are not copied into Git. The
successor evidence is in the archived `animal-lr-recruitment-diagnostics` change and
`runs/output/animal-lr-recruitment-diagnostics/VALIDATION-animal-lr.md`.
