---
# yaml-language-server: $schema=https://raw.githubusercontent.com/nf-core/modules/master/modules/environment-schema.json
# Pure-stdlib Python (applies the 决策 1 gating matrix to JSON metrics). No third-party tool.
channels:
  - conda-forge
dependencies:
  - python=3.12
